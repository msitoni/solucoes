import { CustomerTestimonialsLocale } from './customer-testimonials-locale';

describe('CustomerTestimonialsLocale', () => {
  it('should create an instance', () => {
    expect(new CustomerTestimonialsLocale()).toBeTruthy();
  });
});
